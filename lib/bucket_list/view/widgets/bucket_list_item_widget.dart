import 'package:flutter/material.dart';

class BucketListItemWidget extends StatelessWidget {
  const BucketListItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(
      title: Text('test'),
    );
  }
}
